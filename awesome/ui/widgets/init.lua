return {
	button = require(... .. ".button"),
	container = require(... .. ".container"),
	text = require(... .. ".text"),
	brightness_widget = require(... .. ".brightness-widget.brightness"),
	cpu_widget = require(... .. ".cpu-widget.cpu-widget"),
	volume_widget = require(... .. ".volume-widget.volume"),
	battery_widget = require(... .. ".battery.init"),
	blur = require(... .. ".blur.bgblur")
}
